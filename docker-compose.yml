services:
  app:
    image: ${IMAGE_NAME:-concord:latest}
    container_name: concord-app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
      - NODE_ENV=production
